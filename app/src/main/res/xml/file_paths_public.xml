<?xml version="1.0" encoding="utf-8"?>
<resources>
    <paths>
        <!--
           <files-path/>代表的根目录： Context.getFilesDir()
           <cache-path/>代表的根目录: getCacheDir()
           <external-path/>代表的根目录: Environment.getExternalStorageDirectory()
           <external-files-path/>代表的根目录: Context.getExternalFilesDir(String) Context.getExternalFilesDir(null).
           <external-cache-path />代表的根目录: Context.getExternalCacheDir().
           <root-path />代表设备的根目录new File("/");
           -->
        <!-- path=""代表根目录，也可以指定特定目录，name="camera_picture"是虚拟目录camera_picture -->
        <root-path
            name="root"
            path="" />
        <!--1、对应内部内存卡根目录：Context.getFileDir()-->
        <files-path
            name="files"
            path="" />
        <!--2、对应应用默认缓存根目录：Context.getCacheDir()-->
        <cache-path
            name="cache"
            path="" />
        <!--3、对应外部内存卡根目录：Environment.getExternalStorageDirectory()-->
        <external-path
            name="external"
            path="" />
        <!--4、对应外部内存卡根目录下的APP公共目录：Context.getExternalFileDir(String)-->
        <external-files-path
            name="external_files_path"
            path="" />
        <!--5、对应外部内存卡根目录下的APP缓存目录：Context.getExternalCacheDir()-->
        <external-cache-path
            name="external_cache_path"
            path="" />
        <!--6、对应外部内存卡根目录：Environment.getExternalStorageDirectory()-->
        <external-path
            name="my_images"
            path="Pictures" />
        <!--7、对应外部内存卡根目录：Environment.getExternalStorageDirectory()-->
        <external-path
            name="mitao"
            path="img" />
    </paths>

</resources>